name: Validate Module

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  validate:
    name: Validate Idle Token Animation module files
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Show tool versions
        shell: bash
        run: |
          set -euo pipefail
          node --version
          python3 --version
          jq --version
          zip --version | head -n 2
          unzip -v | head -n 2

      - name: Validate module.json syntax
        shell: bash
        run: |
          set -euo pipefail
          jq . module.json > /dev/null

      - name: Validate required module.json fields
        shell: bash
        run: |
          set -euo pipefail

          required_fields=(
            ".id"
            ".title"
            ".description"
            ".version"
            ".authors"
            ".compatibility.minimum"
            ".compatibility.verified"
            ".esmodules"
            ".styles"
            ".socket"
            ".manifest"
            ".download"
          )

          for field in "${required_fields[@]}"; do
            value="$(jq -r "$field // empty" module.json)"
            if [ -z "$value" ] || [ "$value" = "null" ]; then
              echo "::error::module.json is missing required field: $field"
              exit 1
            fi
          done

      - name: Validate module id and release download filename
        shell: bash
        run: |
          set -euo pipefail

          module_id="$(jq -r '.id' module.json)"
          version="$(jq -r '.version' module.json)"
          manifest="$(jq -r '.manifest' module.json)"
          download="$(jq -r '.download' module.json)"

          if [ "$module_id" != "idle-token-animation" ]; then
            echo "::error::module.json id must be idle-token-animation, got: $module_id"
            exit 1
          fi

          if ! [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::module.json version must look like 0.1.0, got: $version"
            exit 1
          fi

          expected_manifest="https://github.com/${GITHUB_REPOSITORY}/releases/latest/download/module.json"
          expected_download="https://github.com/${GITHUB_REPOSITORY}/releases/download/v${version}/idle-token-animation.zip"

          if [ "$manifest" != "$expected_manifest" ]; then
            echo "::error::manifest URL must be: $expected_manifest"
            echo "::error::got: $manifest"
            exit 1
          fi

          if [ "$download" != "$expected_download" ]; then
            echo "::error::download URL must be: $expected_download"
            echo "::error::got: $download"
            exit 1
          fi

      - name: Validate manifest file references
        shell: bash
        run: |
          set -euo pipefail

          jq -r '.esmodules[]' module.json | while read -r file; do
            if [ ! -f "$file" ]; then
              echo "::error::module.json references missing esmodule: $file"
              exit 1
            fi
          done

          jq -r '.styles[]' module.json | while read -r file; do
            if [ ! -f "$file" ]; then
              echo "::error::module.json references missing stylesheet: $file"
              exit 1
            fi
          done

          if [ ! -f "README.md" ]; then
            echo "::error::README.md missing"
            exit 1
          fi

          if [ ! -f "LICENSE" ]; then
            echo "::error::LICENSE missing"
            exit 1
          fi

      - name: Validate expected folders exist
        shell: bash
        run: |
          set -euo pipefail

          expected_dirs=(
            "scripts"
            "styles"
          )

          for dir in "${expected_dirs[@]}"; do
            if [ ! -d "$dir" ]; then
              echo "::error::Expected folder missing: $dir"
              exit 1
            fi
          done

          if [ ! -d "assets" ]; then
            echo "No assets folder found. Skipping assets."
          fi

      - name: JavaScript syntax check
        shell: bash
        run: |
          set -euo pipefail

          mapfile -t js_files < <(find scripts -type f -name "*.js" | sort)

          if [ "${#js_files[@]}" -eq 0 ]; then
            echo "::error::No JavaScript files found under scripts/"
            exit 1
          fi

          for file in "${js_files[@]}"; do
            echo "Checking JS syntax: $file"
            node --check "$file"
          done

      - name: Validate local JavaScript imports exist
        shell: bash
        run: |
          set -euo pipefail

          cat > /tmp/check-js-imports.py <<'PY'
          import pathlib
          import re
          import sys

          root = pathlib.Path(".").resolve()
          scripts = root / "scripts"

          import_pattern = re.compile(
              r"""(?:import\s+(?:[^'"]+\s+from\s+)?|export\s+[^'"]+\s+from\s+)["']([^"']+)["']"""
          )

          ok = True

          for path in sorted(scripts.rglob("*.js")):
              text = path.read_text(encoding="utf-8")

              for match in import_pattern.finditer(text):
                  specifier = match.group(1)

                  if not specifier.startswith("."):
                      continue

                  target = (path.parent / specifier).resolve()

                  candidates = [
                      target,
                      target.with_suffix(".js") if target.suffix == "" else target,
                      target / "index.js",
                  ]

                  if not any(candidate.exists() for candidate in candidates):
                      rel_path = path.relative_to(root)
                      print(f"::error file={rel_path}::Missing local import target: {specifier}")
                      ok = False

          if not ok:
              sys.exit(1)
          PY

          python3 /tmp/check-js-imports.py

      - name: Validate Handlebars templates
        shell: bash
        run: |
          set -euo pipefail

          if [ ! -d "templates" ]; then
            echo "No templates folder found. Skipping Handlebars validation."
            exit 0
          fi

          mapfile -t hbs_files < <(find templates -type f -name "*.hbs" | sort)

          if [ "${#hbs_files[@]}" -eq 0 ]; then
            echo "No Handlebars templates found under templates/. Skipping Handlebars validation."
            exit 0
          fi

          for file in "${hbs_files[@]}"; do
            echo "Checking template: $file"

            if [ ! -s "$file" ]; then
              echo "::error file=$file::Template is empty"
              exit 1
            fi
          done

      - name: Validate template partial references exist
        shell: bash
        run: |
          set -euo pipefail

          if [ ! -d "templates" ]; then
            echo "No templates folder found. Skipping partial validation."
            exit 0
          fi

          cat > /tmp/check-hbs-partials.py <<'PY'
          import pathlib
          import re
          import sys

          root = pathlib.Path(".").resolve()
          templates = root / "templates"

          partial_pattern = re.compile(r"""\{\{>\s+["']([^"']+)["']\s*\}\}""")

          ok = True

          for path in sorted(templates.rglob("*.hbs")):
              text = path.read_text(encoding="utf-8")

              for match in partial_pattern.finditer(text):
                  partial = match.group(1)

                  if partial.startswith("modules/idle-token-animation/"):
                      target = root / partial.removeprefix("modules/idle-token-animation/")
                  else:
                      target = path.parent / partial

                  if not target.exists():
                      rel_path = path.relative_to(root)
                      print(f"::error file={rel_path}::Missing Handlebars partial: {partial}")
                      ok = False

          if not ok:
              sys.exit(1)
          PY

          python3 /tmp/check-hbs-partials.py

      - name: Build validation zip
        shell: bash
        run: |
          set -euo pipefail

          rm -f idle-token-animation.zip

          zip_args=(
            idle-token-animation.zip
            module.json
            README.md
            LICENSE
            scripts
            styles
          )

          if [ -d "assets" ]; then
            zip_args+=(assets)
          fi

          if [ -d "templates" ]; then
            zip_args+=(templates)
          fi

          zip -r "${zip_args[@]}" \
            -x "*.git*" \
            -x "*.github*"

          if [ ! -s idle-token-animation.zip ]; then
            echo "::error::idle-token-animation.zip was not created"
            exit 1
          fi

          unzip -l idle-token-animation.zip
